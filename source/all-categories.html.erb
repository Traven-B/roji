---
layout: page
title: Category Index
footer_art: search
---
<% topics = blog.articles.group_by {|a| a.metadata[:page][:category] }.delete_if { |category, a| category.nil? } %>

<%= partial 'partials/all-topics',
  locals: {
  topics: topics,
  path_helper: ->(t) { category_path(t) }
} %>
